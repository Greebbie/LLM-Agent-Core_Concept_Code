# Enterprise Documents

This directory is reserved for company-specific documents used when extending the Day 5 Capstone.

The current notebooks use a small in-notebook `KNOWLEDGE_DOCS` corpus by default so the classroom demo is self-contained. To adapt the course for an internal training, add sanitized HR, product, technical support, or policy documents here and update the Capstone loader accordingly.

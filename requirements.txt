# =============================================================================
# LLM-Agent-Core_Concept_Code 依赖配置
# =============================================================================
#
# 安装方式:
#   pip install -r requirements.txt
#
# 或按需安装:
#   pip install torch  # 先按 README 选择 CPU/CUDA 通道
#   pip install transformers  # 核心深度学习
#   pip install sentence-transformers  # RAG embedding
#   pip install openai  # OpenAI API
# =============================================================================

# -----------------------------------------------------------------------------
# 核心依赖 (必需)
# -----------------------------------------------------------------------------
# torch 建议按设备单独安装（CPU/CUDA 渠道不同），见 README
# torch>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.2

# -----------------------------------------------------------------------------
# LLM 后端 (至少选择一个)
# -----------------------------------------------------------------------------

# OpenAI API
openai>=1.0.0

# HuggingFace Transformers (本地模型)
transformers>=4.36.0
accelerate>=0.25.0

# Ollama (推荐用于本地，需要单独安装 Ollama 应用)
# 安装: https://ollama.ai/download
# 拉取模型: ollama pull qwen2.5:7b
requests>=2.31.0

# -----------------------------------------------------------------------------
# Embedding 模型 (RAG 系统)
# -----------------------------------------------------------------------------
sentence-transformers>=2.2.0

# -----------------------------------------------------------------------------
# 训练相关 (SFT/LoRA/DPO)
# -----------------------------------------------------------------------------

# PEFT (LoRA 等高效微调)
peft>=0.7.0

# TRL (SFT/DPO/PPO 训练)
trl>=0.7.0

# Datasets (数据加载)
datasets>=2.16.0

# bitsandbytes (量化，仅 Linux)
# bitsandbytes>=0.41.0

# -----------------------------------------------------------------------------
# Jupyter 环境
# -----------------------------------------------------------------------------
jupyter>=1.0.0
ipywidgets>=8.0.0

# -----------------------------------------------------------------------------
# 其他工具
# -----------------------------------------------------------------------------
tqdm>=4.66.0
scikit-learn>=1.3.0
pandas>=2.0.0

# =============================================================================
# 环境配置说明
# =============================================================================
#
# 1. 使用 Ollama (推荐，免费本地运行):
#    - 下载安装: https://ollama.ai/download
#    - 拉取模型: ollama pull qwen2.5:7b
#    - 运行服务: ollama serve
#
# 2. 使用 OpenAI API:
#    - 设置环境变量: export OPENAI_API_KEY="your-key"
#
# 3. 使用 HuggingFace 模型:
#    - 模型会自动下载到 ~/.cache/huggingface/
#    - 推荐模型: Qwen/Qwen2.5-1.5B-Instruct (小型)
#
# =============================================================================
